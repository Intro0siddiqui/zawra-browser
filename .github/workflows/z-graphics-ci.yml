name: Z-Graphics CI
on:
  push:
    paths:
      - 'dependencies/z-graphics/**'
  pull_request:
    paths:
      - 'dependencies/z-graphics/**'

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libvulkan-dev
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - name: Run Smoke Test
        run: cd dependencies/z-graphics && zig build test
